# Invalid floe.yaml test fixture
# Contains schema violations for testing error messages
#
# Two-Tier Architecture: profile references must be valid strings

# Missing required 'version' field
name: test-pipeline

# Invalid profile reference (must start with letter, not special char)
compute: "123-invalid"

# Invalid transform type
transforms:
  - type: invalid_transform_type
    path: ./dbt
