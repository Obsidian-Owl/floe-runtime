# Valid floe.yaml test fixture
# Used for testing CLI commands with valid configuration
#
# Two-Tier Architecture: Uses profile references (strings) not inline config

name: test-pipeline
version: "1.0.0"

# Profile references - resolved from platform.yaml at compile time
storage: default
catalog: default
compute: default

transforms:
  - type: dbt
    path: ./dbt

governance:
  classification_source: dbt_meta

observability:
  traces: true
  metrics: true
